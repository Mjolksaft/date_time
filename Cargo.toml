[package]
name = "date_time"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13.3", features = ["blocking"] }
time = { version = "0.3", features = ["macros", "local-offset"] }